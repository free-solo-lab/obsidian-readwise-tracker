# Obsidian Readwise Tracker

Standalone Obsidian plugin for tracking reading progress from Readwise.

## Requirements

- `node` 18+
- `npm`

## Development

```bash
npm install
npm test
npm run build
```

Copy `manifest.json`, `main.js`, and `styles.css` into your vault plugin folder.
